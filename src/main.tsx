import React from "react";
import ReactDOM from "react-dom/client";
import Fetti from "./Fetti.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Fetti />
  </React.StrictMode>
);
